{
  "name": "guionaria",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "powershell -ExecutionPolicy Bypass -File scripts/dev.ps1",
    "build": "npm run sidecar:build && npm run tauri -w @guionaria/desktop -- build --config src-tauri/tauri.bundle.conf.json",
    "core:dev": "cd core && uv run guionaria-core serve --reload",
    "core:test": "cd core && uv run pytest -q && uv run ruff check .",
    "sidecar:build": "powershell -ExecutionPolicy Bypass -File scripts/build-sidecar.ps1",
    "typecheck": "npm run typecheck -w @guionaria/desktop",
    "app:test": "npm run test -w @guionaria/desktop",
    "test": "npm run core:test && npm run app:test"
  }
}
